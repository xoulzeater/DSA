
CREATE TABLE "RESOURCE" (
	"RES_ID" INTEGER NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
	"NAME" VARCHAR(30),
	"TYPE" VARCHAR(30),
	"QUANTITY" INTEGER,
	"STATUS" VARCHAR(20)
);

CREATE TABLE "VICTIM" (
	"VICTIM_ID" INTEGER NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
	"NAME" VARCHAR(30),
	"DOB" DATE,
	"ADDRESS" VARCHAR(100),
	"STATUS" VARCHAR(20),
	"GENDER" VARCHAR(20)
	
);


CREATE TABLE "RESOURCEASSIGN" (
	"ID" INTEGER NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
	"RES_ID" INTEGER,
	"VICTIM_ID" INTEGER,
	"ASSIGN_TIME" TIMESTAMP,
	"QUANTITY" INTEGER,
	foreign key ("RES_ID") references "RESOURCE"("RES_ID"),
	foreign key ("VICTIM_ID") references "VICTIM"("VICTIM_ID")
);

CREATE TABLE "TASK" (
	"TASK_ID" INTEGER NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
	"NAME" VARCHAR(30),
	"DESCRIPTION" VARCHAR(200),
	"MANPOWER" INTEGER,
	"PRIORITY" VARCHAR(20),
	"STATUS" VARCHAR(20)
);

CREATE TABLE "STAFF" (
	"STAFF_ID" INTEGER NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
	"NAME" VARCHAR(30),
	"DOB" DATE,
	"STATUS" VARCHAR(30),
	"GENDER" VARCHAR(15)
);

CREATE TABLE "TASKASSIGN" (
	"TASK_ID" INTEGER,
	"STAFF_ID" INTEGER,
	"TIME" TIMESTAMP,
	PRIMARY KEY ("TASK_ID","STAFF_ID")
);

CREATE TABLE "MEDICAL" (
	"MED_ID" INTEGER NOT NULL PRIMARY KEY GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1),
	"TIME" TIMESTAMP,
	"VICTIM_ID" INTEGER,
	"STAFF_ID" INTEGER,
	"DESCRIPTION" VARCHAR(200)
);

INSERT INTO RESOURCE VALUES ('');

INSERT INTO VICTIM VALUES ('');

INSERT INTO RESOURCEASSIGN VALUES ('');

INSERT INTO TASK (NAME,DESCRIPTION,MANPOWER,PRIORITY,STATUS) VALUES ('Fap','Using right hand',5,'Critical','Incomplete');
INSERT INTO TASK (NAME,DESCRIPTION,MANPOWER,PRIORITY,STATUS) VALUES ('Masturbate','Using both hand',5,'Normal','Incomplete');
INSERT INTO TASK (NAME,DESCRIPTION,MANPOWER,PRIORITY,STATUS) VALUES ('Fap V2','Using right hand',5,'Critical','Incomplete');
INSERT INTO TASK (NAME,DESCRIPTION,MANPOWER,PRIORITY,STATUS) VALUES ('Masturbate V2','Using both hand',5,'Normal','Incomplete');
INSERT INTO TASK (NAME,DESCRIPTION,MANPOWER,PRIORITY,STATUS) VALUES ('Wank','Using other people hand',5,'Low','Incomplete');
INSERT INTO TASK (NAME,DESCRIPTION,MANPOWER,PRIORITY,STATUS) VALUES ('Wank V2','Using other people hand',5,'Low','Incomplete');

INSERT INTO STAFF (NAME,DOB,STATUS,GENDER) VALUES ('Chong Zhan Yi','1996-02-24','Available','Male');
INSERT INTO STAFF (NAME,DOB,STATUS,GENDER) VALUES ('Cho Kha Ming','1996-03-23','Available','Male');
INSERT INTO STAFF (NAME,DOB,STATUS,GENDER) VALUES ('Chia Wai Bin','1993-05-09','Available','Male');
INSERT INTO STAFF (NAME,DOB,STATUS,GENDER) VALUES ('Chong Kah Hou','1996-05-27','Available','Male');

INSERT INTO TASKASSIGN VALUES ('');

INSERT INTO MEDICAL ('');